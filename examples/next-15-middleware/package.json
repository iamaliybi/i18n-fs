{
	"name": "example-next-15-middleware",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"i18n": "i18n-fs build --strict",
		"dev": "pnpm i18n && next dev",
		"build": "pnpm i18n && next build",
		"start": "next start",
		"test": "node --test e2e/*.test.mjs"
	},
	"dependencies": {
		"i18n-fs": "workspace:*",
		"next": "^15.5.0",
		"react": "^19.2.0",
		"react-dom": "^19.2.0"
	},
	"devDependencies": {
		"@types/react": "^19.2.2",
		"typescript": "^5.9.3"
	},
	"description": "Next.js 15 with the middleware file convention."
}
