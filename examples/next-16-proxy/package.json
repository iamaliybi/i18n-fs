{
	"name": "example-next-16-proxy",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"i18n": "i18n-fs build --strict",
		"dev": "npm run i18n && next dev",
		"build": "npm run i18n && next build",
		"start": "next start",
		"test": "node --test e2e/*.test.mjs"
	},
	"dependencies": {
		"i18n-fs": "*",
		"next": "^16.0.0",
		"react": "^19.2.0",
		"react-dom": "^19.2.0"
	},
	"devDependencies": {
		"@types/node": "26.2.0",
		"@types/react": "^19.2.2",
		"typescript": "^5.9.3"
	},
	"description": "Next.js 16 with the proxy file convention."
}
