# Changesets

Version bumps are driven by changesets, so `main` always carries an accurate,
human-written changelog and the SemVer bump follows from the change rather than
from someone's memory at release time.

Add one with `npm run changeset` in any pull request that changes published
behaviour. Infrastructure-only pull requests do not need one.
